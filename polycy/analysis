Hi Aravind,

Thanks, that’s helpful. I’ll continue to hold the decommissioning until you’ve completed your checks.

One additional point I noticed while reviewing the environments may help with the decision. The same six database names appear across three SQL servers/environments:

sql-
sql
sql

The six databases on bst-iaz-sql-dev-geo are currently Online, and the same database names also exist on vos-iaz-sql-dev-geo (with five Online and OSMM currently Paused). The corresponding six databases on [datbasename] are currently Inaccessible.

Given the context you’ve provided for SWEET and FMEShared, could you please confirm whether [databasename] is still required, or whether the active/current versions of these databases are now hosted on one of the other two servers?

I’ve attached screenshots showing the database lists/status across the three servers for comparison.

Once you’ve completed the checks on the remaining databases, we can confirm what can safely be decommissioned.

Kind regards,
Cynthia