Tasks

1. Confirm the Key Vault permission gap. Retrieve the server's managed identity object ID and search all pages of the access policies on bst-iaz-kvt-dev-geo. Required permissions: Get, WrapKey, UnwrapKey.

az sql server show -g C190VOArgpBSTDev \
  -n vos-iaz-sql-dev-arc11x-geo \
  --query identity.principalId -o tsv

2. Establish when access was lost. Cost data shows normal billing through 31/07/2026 and a drop in August, so the failure falls within August 2026. Check Resource Health on the server, and the vault's Activity log for permission changes. Also check resource group Deployments for template runs that may have replaced the access policy array.

3. Identify the vault used by geoedserver, and check the TDE configuration of bsttestserver — not yet examined.

4. ⚠️ Verify with the geospatial team whether the arc11x environment is still required. This is the blocking item. Naming (arc11x) and dataset names (OSAddressBase, OSMM, GeoLocators, GeoCuratedData) suggest an ArcGIS 11.x environment. An identical, functional environment exists on bst-iaz-sql-dev-geo. No assumption should be made about whether arc11x is redundant — this needs confirming with the owners, not inferring.

5. Decide and act: restore Key Vault access and revalidate the key (SQL server → Transparent data encryption → Validate key), or delete with documented owner sign-off.

⚠️ If the vault is managed by Bicep or ARM, the permission fix must go into the template — a portal change will be reverted on the next deployment.

Acceptance criteria

Permission status confirmed for all three servers
Failure date and cause identified, or documented as not determinable
Geospatial team's decision recorded in this ticket
All eight databases either Online or deleted with documented sign-off